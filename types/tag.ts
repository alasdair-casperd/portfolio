export type Tag = {
  // A name used to refer to the tag
  name: string;

  // Text to display in the tag
  tag_text: string;

  // The name of the icon to use for the tag
  icon: string;

  // A tailwind css colour to use for the tag
  colour: string;
};
